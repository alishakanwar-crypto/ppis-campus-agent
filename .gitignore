__pycache__/
*.pyc
snapshots/
face_images/
attendance_snapshots/
*.db
agent.lock
.agent_lock
.locks/
wrapper_*.log*
# The agents keep these open while they run, so a self-update cannot
# delete them on Windows; untracked they never block a pull again.
*.log

# Per-machine local config and persistent counter state.
# Not tracked so restart scripts (git reset --hard) never overwrite them.
config.json
cpplus_replay_state.json
cpplus_segment_replay_state.json
cpplus_native_summary_state.json
cpplus_native_summary_pending.json
cpplus_recordings/
face_audit_results/
snapshot_doors.json
